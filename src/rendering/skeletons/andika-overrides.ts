import type { SkeletonOverrides } from "./types";

/**
 * Hand-authored skeleton overrides. Each entry replaces the corresponding
 * baseline entry at module-load time (see ./andika.ts for the merge logic).
 *
 * Authored by the LLM workflow described in skills/improve-centerlines/SKILL.md.
 *
 * Empty until Phase 4 of the improve-centerlines plan begins.
 */
const OVERRIDES: SkeletonOverrides = {};

export default OVERRIDES;
